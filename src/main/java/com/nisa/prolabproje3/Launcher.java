package com.nisa.prolabproje3;


public class Launcher {
    public static void main(String[] args) {
       // Since the actual main class extends the application class, it cannot run directly.
       // That's why we created a launcher class.
        ProlabProje3.main(args);
    }
}